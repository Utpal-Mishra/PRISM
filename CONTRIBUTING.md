# Contributing

1. Branch from `develop` using `agent/<scope>`.
2. Add or update tests.
3. Run `pytest -q` and `ruff check .`.
4. Update README and CHANGELOG for user-facing changes.
5. Open a pull request into `develop`; release pull requests target `main`.
