(() => {
  "use strict";
  const STORE_KEY="prism.decisions.v1", METHOD_VERSION="decision-register-1.0.0", CLASSIFICATION="organisational decision record; human-approved action; causal-not-established";
  let decisions=[];
  document.addEventListener("DOMContentLoaded",()=>setTimeout(init,0));
  function init(){const view=document.getElementById("view-decision");if(!view)return;document.getElementById("createDecision")?.addEventListener("click",createDecision);document.getElementById("downloadDecisionRecord")?.addEventListener("click",downloadLatest);document.getElementById("clearDecisionRegister")?.addEventListener("click",clearAll);load();render();setTimeout(prefill,250);}
  function state(){return window.PRISM_STATE||window.prismState||null;} function rows(){return state()?.data||[];}
  function dataset(){const name=document.getElementById("datasetName")?.textContent?.trim()||"dataset";return{name,id:`DS-${slug(name).toUpperCase().slice(0,32)}`,rows:rows().length,source_mode:document.getElementById("datasetMode")?.textContent?.trim()||"Browser-local"};}
  function load(){try{const v=JSON.parse(localStorage.getItem(STORE_KEY)||"[]");decisions=Array.isArray(v)?v:[];}catch(_e){decisions=[];}} function save(){try{localStorage.setItem(STORE_KEY,JSON.stringify(decisions.slice(0,50)));}catch(_e){}}
  function strategy(){try{return JSON.parse(localStorage.getItem("prism.strategy.v2")||"null");}catch(_e){return null;}}
  function prefill(){const s=strategy();if(!s)return;set("decisionOwner",s.owner?.startsWith("Unassigned")?"":s.owner);set("decisionReview",s.review_date||"");set("decisionAction",s.recommended_action||"");set("decisionAlternative",s.alternatives?.[0]||"");set("decisionOutcome",s.expected_impact?.statement||"");const hint=document.getElementById("decisionStrategyHint");if(hint)hint.textContent=`Linked strategy available: ${s.strategy_id}. Review and approve the decision fields before recording.`;}
  function createDecision(){const s=strategy(),owner=value("decisionOwner"),action=value("decisionAction"),review=value("decisionReview"),outcome=value("decisionOutcome"),alternative=value("decisionAlternative");if(!owner||!action||!review){notice("Owner, selected action and review date are required. PRISM will not create an unaccountable decision record.");return;}if(!s){notice("Build a Strategy Engine 2.0 proposal first so the decision can retain strategy and evidence traceability.");return;}if(!s.strategy_id||!Array.isArray(s.evidence_ids)||!s.evidence_ids.length){notice("The linked strategy lacks evidence IDs. Rebuild the strategy proposal before recording a decision.");return;}const now=new Date(),d={decision_id:`DEC-${stamp(now)}-${slug(s.kpi_to_monitor||"decision").toUpperCase().slice(0,12)}`,dataset:dataset(),strategy_id:s.strategy_id,evidence_ids:s.evidence_ids,owner,alternatives_considered:[alternative||"No additional alternative recorded"],selected_action:action,expected_outcome:outcome||"Outcome not quantified; define through Scenario Studio or controlled validation.",kpi_to_monitor:s.kpi_to_monitor||null,status:"Approved for bounded validation — human owner recorded",review_date:review,decision_basis:{problem:s.problem,hypothesis:s.hypothesis,confidence:s.confidence,risk:s.risk,method_version:s.method_version,classification:s.classification},method_version:METHOD_VERSION,classification:CLASSIFICATION,assumptions:["The accountable owner has reviewed the linked strategy and evidence before recording this decision.","Expected outcome is a decision expectation, not proof of causal effect."],limitations:["Browser-local persistence is not a substitute for enterprise records management or access control.","PRISM does not verify the identity or authority of the named owner in the static edition.","Observed evidence and scenario sensitivity do not establish causal impact."],created_at:now.toISOString(),source_mode:dataset().source_mode};decisions.unshift(d);save();render();notice(`Decision ${d.decision_id} recorded locally with linked strategy and evidence.`);}
  function render(){const host=document.getElementById("decisionRegister");if(!host)return;if(!decisions.length){host.innerHTML='<div class="insight-item"><strong>No decisions recorded</strong><p>Create a reviewed Strategy Engine 2.0 proposal, then record the accountable decision here.</p></div>';return;}host.innerHTML=decisions.slice(0,12).map(d=>`<article class="strategy-card"><p class="eyebrow">${esc(d.decision_id)}</p><h3>${esc(d.kpi_to_monitor||"Decision")}</h3><p><strong>Action:</strong> ${esc(d.selected_action)}</p><p><strong>Owner:</strong> ${esc(d.owner)} · <strong>Review:</strong> ${esc(d.review_date)}</p><p><strong>Traceability:</strong> ${esc(d.strategy_id)} · ${esc(d.evidence_ids.join(", "))}</p><p><strong>Status:</strong> ${esc(d.status)}</p><small>${esc(d.classification)}</small></article>`).join("");}
  function downloadLatest(){if(!decisions.length){notice("Record a decision before downloading a Decision Record.");return;}const d=decisions[0],now=new Date(),name=`PRISM_DECISION_${slug(d.dataset.name)}_${fileStamp(now)}.json`;download(name,{prism_version:"1.2.0",artifact:"Decision Record",...d});}
  function clearAll(){decisions=[];save();render();notice("Browser-local Decision Register cleared.");} function notice(msg){const n=document.getElementById("notice");if(n){n.textContent=msg;n.classList.remove("hidden");setTimeout(()=>n.classList.add("hidden"),6000);}} function set(id,v){const e=document.getElementById(id);if(e&&!e.value)e.value=v;} function value(id){return(document.getElementById(id)?.value||"").trim();}
  function download(name,obj){const b=new Blob([JSON.stringify(obj,null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1000);} function slug(v){return String(v||"dataset").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"dataset";} function stamp(d){return d.toISOString().replace(/[-:TZ.]/g,"").slice(0,14);} function fileStamp(d){const p=n=>String(n).padStart(2,"0");return`${d.getFullYear()}${p(d.getMonth()+1)}${p(d.getDate())}-${p(d.getHours())}${p(d.getMinutes())}`;} function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));}
})();

/* Stage 10 integration: keep the static edition modular while adding the Outcome feedback view. */
(() => {
  "use strict";
  document.addEventListener("DOMContentLoaded",()=>{
    const decisionView=document.getElementById("view-decision"), qualityView=document.getElementById("view-quality");
    if(!decisionView||!qualityView||document.getElementById("view-outcome"))return;
    const section=document.createElement("section");section.id="view-outcome";section.className="view";section.innerHTML=`<div class="section-intro"><div><p class="eyebrow">LEARN</p><h2>Outcome Feedback</h2><p>Compare the recorded expectation with an observed KPI result and capture what the organisation learned. Alignment is descriptive; it does not prove the selected action caused the outcome.</p></div><button id="downloadOutcomeReview" class="secondary-button" type="button">Download Outcome Review</button></div><article class="panel"><div class="control-row three outcome-controls"><label>Decision<select id="outcomeDecision"></select></label><label>KPI<input id="outcomeKpi" type="text" maxlength="100" placeholder="KPI definition"/></label><label>Expected KPI<input id="outcomeExpected" type="number" step="any" placeholder="Expected value"/></label><label>Actual KPI<input id="outcomeActual" type="number" step="any" placeholder="Observed value"/></label><label>Evaluation start<input id="outcomeStart" type="date"/></label><label>Evaluation end<input id="outcomeEnd" type="date"/></label><label>Learning<textarea id="outcomeLearning" placeholder="What should future decisions learn from this result?"></textarea></label><button id="evaluateOutcome" class="primary-button" type="button">Evaluate outcome</button></div><div class="decision-actions"><span class="tag">Browser-local learning loop</span><button id="clearOutcomeReviews" class="secondary-button" type="button">Clear local reviews</button></div></article><article class="panel"><div class="panel-heading"><div><p class="eyebrow">OUTCOME MEMORY</p><h2>Decision reviews</h2></div></div><div id="outcomeReviews" class="decision-grid"></div></article>`;qualityView.parentNode.insertBefore(section,qualityView);
    const side=document.querySelector('.nav-list .nav-item[data-view="quality"]');if(side){const b=document.createElement("button");b.className="nav-item";b.dataset.view="outcome";b.type="button";b.textContent="Outcome Feedback";side.parentNode.insertBefore(b,side);}
    const dock=document.querySelector('.mobile-dock .nav-item[data-view="quality"]');if(dock){const b=document.createElement("button");b.className="nav-item mobile-dock-item";b.dataset.view="outcome";b.type="button";b.innerHTML='<span aria-hidden="true">◎</span><small>Outcome</small>';dock.parentNode.insertBefore(b,dock);}
    const style=document.createElement("style");style.textContent='.outcome-controls input,.outcome-controls textarea{min-height:44px}.outcome-controls textarea{min-height:88px;resize:vertical}@media(max-width:600px){.outcome-controls{grid-template-columns:1fr!important}.outcome-controls button{width:100%}}';document.head.appendChild(style);
    const version=document.getElementById("versionLabel");if(version)version.textContent="v1.3.0 · GitHub Pages";
    const script=document.createElement("script");script.src="./assets/outcome-feedback.js";script.defer=true;document.body.appendChild(script);
  });
})();
